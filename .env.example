# Local dev + Vercel (Project → Settings → Environment Variables)
# Never commit .env — never use VITE_ prefix (keeps key server-side only)
OPENROUTER_API_KEY=your_api_key_here
OPENROUTER_MODEL=openrouter/free
